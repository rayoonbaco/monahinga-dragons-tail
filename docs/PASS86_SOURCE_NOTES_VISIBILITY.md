# Pass 86 — Source Notes Visibility Fix

Recorded: 2026-06-04 21:20:05

The Build card runtime was working again after the matched runtime restore, but the Optional Source Notes / Evidence Meter area was not visible enough in the active page flow.

Pass 86 did not rewrite the JavaScript runtime.

It moved the evidence intake block directly inside the main question card, immediately under the Build card row, so the user can see and use source notes before building the card.

This pass intentionally avoided new features and avoided QA/test-button clutter.

Backed up previous files to:

```text
C:\PROJECTS\DRAGON'S TAIL\backups\dragon_tail_pass86_source_notes_visibility_20260604_212005
```
